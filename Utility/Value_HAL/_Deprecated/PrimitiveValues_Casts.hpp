#pragma once

#include "PrimitiveValues.hpp"


Context(NotationEngine::Utility::Value)

SSource 
{
	//sfn CastToNative8(ro Ref(Native16) Native16ToCast) -> Native8;
	//sfn CastToNative8(ro Ref(Native32) Native32ToCast) -> Native8;
	//sfn CastToNative8(ro Ref(Native64) Native64ToCast) -> Native8;
  //sfn CastToNative8(readonly Ref(ANativeEP) NativeEPToCast) -> APtr<Native8>;
  //sfn CastToNative8(readonly Ref(APtr<AGMPNum  >) AGMPNumToCast ) -> APtr<AGMPNum>;
  //sfn CastToNative8(readonly Ref(APtr<ABoostNum>) BoostNumToCast) -> APtr<Native8>;
	
    //sfn CastToNative16(ro Ref(Native8 ) Native8ToCast ) -> Native16;
	//sfn CastToNative16(ro Ref(Native32) Native32ToCast) -> Native16;
	//sfn CastToNative16(ro Ref(Native64) Native64ToCast) -> Native16;
  //sfn CastToNative16(readonly Ref(APtr<ANativeEP>) NativeEPToCast) -> APtr<Native16>;
  //sfn CastToNative16(readonly Ref(APtr<AGMPNum  >) AGMPNumToCast ) -> APtr<Native16>;
  //sfn CastToNative16(readonly Ref(APtr<ABoostNum>) BoostNumToCast) -> APtr<Native16>;

	//sfn CastToNative32(ro Ref(Native8 ) Native8ToCast ) -> Native32;
	//sfn CastToNative32(ro Ref(Native16) Native16ToCast) -> Native32;

	//sfn CastToNative64(ro Ref(Native8 ) Native8ToCast ) -> Native64;
	//sfn CastToNative64(ro Ref(Native16) Native16ToCast) -> Native64;
}

Context_End
